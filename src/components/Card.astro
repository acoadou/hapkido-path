---
interface Props { title?: string; href?: string; }
const { title, href } = Astro.props;
---
<article class="card generic-card">
  {title && (href ? <h3><a href={href}>{title}</a></h3> : <h3>{title}</h3>)}
  <slot />
</article>
